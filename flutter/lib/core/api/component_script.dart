/// Class which defines a script to be injected for a component.
///
/// [file] path to the script file.
class ComponentScript {
  final String file;

  const ComponentScript({required this.file});
}
